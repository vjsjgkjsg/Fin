// categories.js
// Категории доходов и расходов

const categories = (() => {

    const render = () => {
        // Отрисовка категорий
    };

    const add = (data) => {
        // Добавить категорию
    };

    const remove = (id) => {
        // Удалить категорию
    };

    const update = (id, data) => {
        // Редактировать категорию
    };

    return {
        render,
        add,
        remove,
        update
    };
})();
