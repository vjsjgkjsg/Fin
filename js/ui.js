// ui.js
// Весь интерфейс и события

const ui = (() => {

    const init = () => {
        // Навешиваем события
    };

    const openModal = (id) => {
        // Открыть модалку
    };

    const closeModal = (id) => {
        // Закрыть модалку
    };

    return {
        init,
        openModal,
        closeModal
    };
})();
