// filters.js
// Фильтрация, поиск, сортировка

const filters = (() => {

    const byDate = (from, to) => {
        // Фильтр по дате
    };

    const byText = (text) => {
        // Поиск по операциям
    };

    const sort = (type) => {
        // new | old
    };

    return {
        byDate,
        byText,
        sort
    };
})();
