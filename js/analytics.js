// analytics.js
// Аналитика и графики

const analytics = (() => {

    const renderCharts = () => {
        // График расходов
    };

    const calculateStats = () => {
        // Статистика по категориям
        return {};
    };

    return {
        renderCharts,
        calculateStats
    };
})();
