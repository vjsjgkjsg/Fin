// records.js
// Операции (доходы / расходы)

const records = (() => {

    const add = (record) => {
        // Добавить запись
    };

    const remove = (id) => {
        // Удалить запись
    };

    const update = (id, data) => {
        // Редактировать запись
    };

    const getAll = () => {
        return [];
    };

    return {
        add,
        remove,
        update,
        getAll
    };
})();
